apiVersion: v1
kind: Service
metadata:
  name: {{ include "self-hosted-app.fullname" . }}
  labels:
    {{- include "self-hosted-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "self-hosted-app.selectorLabels" . | nindent 4 }}
